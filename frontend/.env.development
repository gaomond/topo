# ローカル開発時の API ベース URL（別オリジンのバックエンド）。
# フロント（Vite dev server :5173）から別オリジンの API（:8080）を CORS 経由で呼ぶ。
# 未設定だと同一オリジン（:5173）へ発射され、Vite が静的ファイル(304)を返して config 取得に失敗する。
# デプロイ先は各環境の .env で上書きする。
VITE_API_BASE_URL=http://localhost:8080
